scope: global
author:
    framework: noronha

project:
    working_project: null
docker:
    daemon_address: unix:/var/run/docker.sock  # docker's default unix socket
    target_registry: null  # if set, project images will be pushed after each build
    manager:
      type: swarm
file_manager:
    native: true
    type: artif
mongo:
    native: true
router:
    native: true
logger:
    level: INFO
    pretty: false
    join_root: true
    traceback_style: darkbg2
predict:
    online:
